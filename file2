[200~Open an SSH client.

Locate your private key file. The key used to launch this instance is mumkey.pem

Run this command, if necessary, to ensure your key is not publicly viewable.
chmod 400 "mumkey.pem"
Connect to your instance using its Public DNS:
ec2-13-204-67-178.ap-south-1.compute.amazonaws.com
Example:

ssh -i "mumkey.pem" ec2-user@ec2-13-204-67-178.ap-south-1.compute.amazonaws.com
